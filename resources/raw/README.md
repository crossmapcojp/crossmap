# Raw acquisition inputs

This directory is reserved for immutable seed and acquisition inputs. Runtime and build code must not depend on the former gmap project; copied HTTP cache files are stored by content hash under `resources/crawl/pages`.
